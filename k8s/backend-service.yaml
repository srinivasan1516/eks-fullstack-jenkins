apiVersion: v1
kind: Service
metadata:
  name: backend-service
  namespace: fullstack-app
spec:
  type: ClusterIP     # internal only — the frontend's nginx proxies to this
  selector:
    app: backend
  ports:
    - port: 5000
      targetPort: 5000
      protocol: TCP
